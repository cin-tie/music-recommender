apiVersion: v1
kind: Service
metadata:
  name: spotify-service
  labels:
    app: spotify-service
spec:
  selector:
    app: spotify-service
  ports:
    - port: 80
      targetPort: 8800
  type: ClusterIP